# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Gift Exchange' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Fabric'
  pod 'Crashlytics'

  # Pods for Gift Exchange

  target 'Gift ExchangeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Gift ExchangeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
